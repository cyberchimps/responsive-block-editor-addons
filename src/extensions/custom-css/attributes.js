/**
 * Custom CSS Extension Attributes
 * Adds customCss attribute to all blocks
 */

const CustomCSSAttributes = {
  customCss: {
    type: 'string',
    default: '',
  },
};

export default CustomCSSAttributes;

